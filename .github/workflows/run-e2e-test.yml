name: Run E2E Tests

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [ main ]
  workflow_call:
    outputs:
      success:
        description: 'Whether the e2e tests passed successfully'
        value: ${{ jobs.e2e-tests.outputs.success }}
      summary:
        description: 'High-level summary of test results'
        value: ${{ jobs.e2e-tests.outputs.summary }}
      run_url:
        description: 'URL to this workflow run for detailed logs'
        value: ${{ jobs.e2e-tests.outputs.run_url }}

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    outputs:
      success: ${{ steps.test-results.outputs.success }}
      summary: ${{ steps.test-results.outputs.summary }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Create kind config
        run: |
          cat > kind-config.yaml << EOF
          kind: Cluster
          apiVersion: kind.x-k8s.io/v1alpha4
          containerdConfigPatches:
          - |-
            [plugins."io.containerd.grpc.v1.cri".registry]
              config_path = "/etc/containerd/certs.d"
          nodes:
          - role: control-plane
            kubeadmConfigPatches:
            - |
              kind: InitConfiguration
              nodeRegistration:
                kubeletExtraArgs:
                  system-reserved: memory=500Mi
                  eviction-hard: memory.available<200Mi
            extraPortMappings:
            - containerPort: 80
              hostPort: 80
              protocol: TCP
            - containerPort: 443
              hostPort: 443
              protocol: TCP
          EOF

      - name: Create k8s Kind Cluster
        id: kind
        uses: helm/kind-action@ef37e7f390d99f746eb8b610417061a60e82a6cc # v1.14.0
        with:
          registry: true
          registry_name: kind-registry
          registry_port: 5000
          registry_enable_delete: true
          config: kind-config.yaml

      - name: Build operator image
        run: |
          # Build the image with run-based tag for uniqueness
          docker build -t kind-registry:5000/ogx-k8s-operator:run-${{ github.run_id }} -f Dockerfile .

          # Tag the image for local registry
          docker tag kind-registry:5000/ogx-k8s-operator:run-${{ github.run_id }} kind-registry:5000/ogx-k8s-operator:latest

      - name: Push operator image to local registry
        run: |
          echo '{"insecure-registries": ["kind-registry:5000"]}' | sudo tee /etc/docker/daemon.json
          sudo systemctl restart docker
          until docker exec kind-registry true 2>/dev/null; do sleep 1; done
          docker push kind-registry:5000/ogx-k8s-operator:latest

      - name: Install cert-manager
        run: |
          kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.17.2/cert-manager.yaml
          kubectl wait --for=condition=available --timeout=120s deployment/cert-manager -n cert-manager
          kubectl wait --for=condition=available --timeout=120s deployment/cert-manager-webhook -n cert-manager
          kubectl wait --for=condition=available --timeout=120s deployment/cert-manager-cainjector -n cert-manager

      - name: Deploy operator
        run: |
          # Deploy the operator
          make deploy IMG=kind-registry:5000/ogx-k8s-operator:latest

          # Wait for operator deployment to be ready
          if ! kubectl wait --for=condition=available --timeout=300s deployment/ogx-k8s-operator-controller-manager -n ogx-k8s-operator-system; then
            echo "Deployment failed to become ready. Debugging information:"
            kubectl describe deployment ogx-k8s-operator-controller-manager -n ogx-k8s-operator-system
            kubectl logs -l control-plane=controller-manager -n ogx-k8s-operator-system --tail=100
            kubectl get events -n ogx-k8s-operator-system --sort-by='.lastTimestamp'
            exit 1
          fi

      - name: Run e2e tests
        run: |
          make test-e2e

      - name: Set test results
        id: test-results
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "success=true" >> $GITHUB_OUTPUT
            echo "summary=E2E tests passed successfully" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
            echo "summary=E2E tests failed - check logs for details" >> $GITHUB_OUTPUT
          fi

      - name: Get logs
        if: ${{ always() }}
        run: |
          kubectl -n ogx-test get all -o yaml > all.log
          kubectl -n ogx-k8s-operator-system logs deployment.apps/ogx-k8s-operator-controller-manager > controller-manager.log
          kubectl -n ogx-k8s-operator-system logs deployment.apps/ogx-k8s-operator-controller-manager --previous > controller-manager-previous.log 2>/dev/null || true
          kubectl -n ogx-test describe all > all-describe.log
          kubectl -n ogx-test describe events > events.log
          kubectl get ogxservers --all-namespaces -o yaml > ogxservers.log
          echo "=== Operator Pod Status ===" > operator-debug.log
          kubectl -n ogx-k8s-operator-system get pods -o wide >> operator-debug.log 2>&1
          echo "=== Operator Pod Describe ===" >> operator-debug.log
          kubectl -n ogx-k8s-operator-system describe pods -l control-plane=controller-manager >> operator-debug.log 2>&1
          echo "=== Node Memory Pressure ===" >> operator-debug.log
          kubectl describe nodes | grep -A5 "MemoryPressure\|memory\|OOMKill" >> operator-debug.log 2>&1
          echo "=== Operator Events ===" >> operator-debug.log
          kubectl -n ogx-k8s-operator-system get events --sort-by='.lastTimestamp' >> operator-debug.log 2>&1

      - name: Upload all logs to artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: logs-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            *.log
          retention-days: 1
